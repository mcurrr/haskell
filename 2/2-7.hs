--3
n = a `div` length xs
    where
        a = 10
        xs = [1,2,3,4,5]

--4
myLast xs = head (reverse xs)
my2Last xs = xs !! (length xs - 1)

type Triangle = {Int, Int, Int}

test () ->
    A = hello
    B = bye
    A 

a
b

test1(a, a)
test1(a, b)

class Bank {

    var deposit = 20

    def withdraw(a: Int): Int = {
        if (a > deposit) throw Error("")
        else {
            deposit - a
            deposit
        }
    }

}